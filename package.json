{
    "name": "gate-entry-system-root",
    "private": true,
    "engines": {
        "node": ">=20"
    },
    "scripts": {
        "install": "npm install --prefix web-app",
        "build": "npm run build --prefix web-app"
    }
}